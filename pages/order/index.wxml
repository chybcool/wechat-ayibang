<view class="container">
    <image src="{{images}}"></image>
    <text class="nologin" bindtap="bindViewTap">{{nologin}}</text>
    <text class="login" bindtap="bindViewTap">{{login}}</text>
</view>
